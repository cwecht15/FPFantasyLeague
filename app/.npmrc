# next-auth v5 (beta) declares a peer range of Next 14/15 that lags behind the
# Next 16 we run (same Next version as Data_Suite_2.0). The library works fine
# with Next 16; this avoids the spurious ERESOLVE peer conflict on install.
legacy-peer-deps=true
